app:
  registration-period-in-millis: 50

hubspot:
  host: http://localhost:9999
  apiKey: some-api-key
  marketingSubscriptionId: 5911726

googlerecaptcha:
  host: http://localhost:9999
  threshold: 0.5
  secretKey: secret-key

subjects:
  cache:
    timetolive: 5000

spring:
  cloud:
    gcp:
      pubsub:
        enabled: false
